CREATE TABLE CossStudent
  (
   id bigint(20) NOT NULL AUTO_INCREMENT,
   cossName  varchar(40)  NOT NULL,
   cossEmail varchar(40)  NOT NULL,
   cossMobile varchar(20)  NOT NULL,
    PRIMARY KEY (id)
 );